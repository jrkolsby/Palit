fn render(
        muted: bool,
        armed: bool,
        soloed: bool
    ) -> String {
    return "Hi".to_string();
}

struct Track {
    muted: bool,
    armed: bool,
    solo: bool,
}

impl cursive::view::View for Track {
    pub draw() -> Self {
        for (i, pair)
    }
}
