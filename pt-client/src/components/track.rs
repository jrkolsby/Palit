mod waveform;

fn render(
        muted: bool,
        armed: bool,
        soloed: bool
    ) -> String {
    return "Hi".to_string();
}

struct Track {
    muted: bool,
    armed: bool,
    solo: bool,
}

impl Track {
    pub fn new() -> Self {
        let 
    }
}
